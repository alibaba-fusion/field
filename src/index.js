import React, { Component } from 'react';

export default class Field extends Component {
  static displayName = 'Field';

  render() {
    return (
      <div className="field">Hello Field</div>
    );
  }
}
